// AUTO-GENERATED by event-schema. Do not edit.
// Source: examples/experiments/event-schema.yaml

export type AnalyticsEvents = {
  /**
   * Account creation succeeded.
   */
  signup_completed: {
    plan: "free" | "pro";
  };

  /**
   * Canonical exposure event fired the first time a user is bucketed
   * into an experiment. Wire whichever event name your platform uses
   * ($exposure on Amplitude, $experiment_started on Mixpanel) — the
   * property shape is the same.
   */
  exposure: {
    /** Experiment name. Should match a key in the experiments section. */
    flag_key: string;
    /** Variant name from the experiment's variants list. */
    variant: string;
  };
};

export type ExperimentName =
  | "pricing_redesign_2026"
  | "checkout_flow_2026";

export type Variant<E extends ExperimentName> = {
  /**
   * A/B test on the /pricing hero — long-form copy vs short-form
   * with an embedded video.
   */
  pricing_redesign_2026: "control" | "long_copy" | "short_video";
  /**
   * Single-page vs multi-step checkout. Measures completion rate
   * and average time-to-pay.
   */
  checkout_flow_2026: "control" | "single_page";
}[E];
